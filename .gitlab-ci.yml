stages:
  - fetch
  - build
  - deploy


fetch_dev:
  stage: fetch
  tags:
    - dev
  variables:
    GIT_STRATEGY: fetch
    GIT_CLONE_PATH: /home/bitnami/builds/hims-app-dev
  script:
    - echo "This job fetches hims_app dev branch. "
    - echo "should run ~/copy_clone_to_dev.sh"
    - cd ~/hims-app-dev
    - yarn install

build_dev:
  stage: build
  variables:
    GIT_STRATEGY: none
  tags:
    - dev
  needs: ["fetch_dev"]
  script:
    - echo "This job builds hims-dev after fetch_dev"
    - cd ~/hims-app-dev
    - yarn run build



deploy_dev:
  stage: deploy
  variables:
    GIT_STRATEGY: none
  tags:
    - dev
  needs: ["build_dev"]
  script:
    - echo "Deploy the hims-dev"
    - echo "Needs hims-dev to have passed build stage"



